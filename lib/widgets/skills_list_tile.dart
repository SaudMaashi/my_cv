import 'package:flutter/material.dart';

class SkillsListTile extends StatelessWidget {
  const SkillsListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
